-- Add redirect URL configuration for the custom domain
-- This is a placeholder comment as the actual redirect URL configuration
-- is handled via Supabase dashboard settings, but we ensure the domain is documented

-- Update comment on profiles table to document the configured domain
COMMENT ON TABLE public.profiles IS 'User profiles for Buwate Tennis Club. Auth redirect domain: https://buwatetc.onrender.com';